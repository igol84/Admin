import React from 'react';
import {Box} from "@mui/material";

const SaleLineItems = () => {
  return (
    <Box  sx={{border: 'solid', flex: 1}}>
      SaleLineItems
    </Box>
  );
};

export default SaleLineItems;